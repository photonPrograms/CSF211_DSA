/* stack.h */

#include "linkedlist.h"

// insert element in front (at first position)
void push(struct linkedList *head, int ele);

// delete the first element
struct node* pop(struct linkedList *head);
